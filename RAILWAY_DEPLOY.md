# คำแนะนำการ Deploy บน Railway

Railway เป็น platform ที่ง่ายและรวดเร็วสำหรับ deploy applications โดยไม่ต้องจัดการ infrastructure เอง

## 📋 ข้อกำหนดเบื้องต้น

1. บัญชี Railway (สมัครได้ที่ [railway.app](https://railway.app))
2. GitHub repository ที่มีโค้ดอยู่แล้ว
3. (Optional) Railway CLI สำหรับ deploy แบบ local

## 🚀 วิธี Deploy

### วิธีที่ 1: Deploy ผ่าน Railway Dashboard (แนะนำ)

1. **Login เข้า Railway**
   - ไปที่ [railway.app](https://railway.app)
   - Login ด้วย GitHub account

2. **สร้าง Project ใหม่**
   - คลิก "New Project"
   - เลือก "Deploy from GitHub repo"
   - เลือก repository ของคุณ
   - เลือก branch (แนะนำ: `main` หรือ `master`)

3. **Railway จะ detect โค้ดอัตโนมัติ**
   - Railway จะอ่าน `Procfile` หรือ `railway.json` อัตโนมัติ
   - จะติดตั้ง dependencies จาก `requirements.txt`
   - จะรัน app ตาม start command

4. **เพิ่ม PostgreSQL Database**
   - ใน Railway Dashboard → คลิก "+ New"
   - เลือก "Database" → "Add PostgreSQL"
   - Railway จะสร้าง `DATABASE_URL` อัตโนมัติ

5. **ตั้งค่า Environment Variables**
   - ไปที่ Settings → Variables
   - เพิ่มตัวแปรต่อไปนี้:
     - `SECRET_KEY`: สร้าง secret key ใหม่ (ใช้ `openssl rand -hex 32` หรือ generate online)
     - `FLASK_ENV`: `production` (optional)
     - `DATABASE_URL`: จะถูกสร้างอัตโนมัติเมื่อเพิ่ม PostgreSQL (ไม่ต้องตั้งเอง)

6. **Deploy**
   - Railway จะ deploy อัตโนมัติเมื่อ push code ใหม่
   - หรือคลิก "Deploy" ใน Dashboard

### วิธีที่ 2: ใช้ Railway CLI

```bash
# ติดตั้ง Railway CLI
npm i -g @railway/cli

# Login
railway login

# Link project
railway link

# Deploy
railway up
```

## 🔧 Configuration Files

### Procfile
```
web: gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile - --error-logfile - app:app
```

### railway.json
ไฟล์นี้จะช่วยให้ Railway รู้ว่าต้อง build และ deploy อย่างไร

## 📝 Environment Variables

| Variable | Required | Description | Example |
|----------|----------|-------------|---------|
| `SECRET_KEY` | ✅ Yes | Flask secret key สำหรับ session | `your-secret-key-here` |
| `DATABASE_URL` | ✅ Yes | PostgreSQL connection string | Auto-generated by Railway |
| `FLASK_ENV` | ❌ No | Flask environment | `production` |
| `PORT` | ❌ No | Port number (auto-set by Railway) | Auto-set |

## 🗄️ Database Setup

Railway จะสร้าง PostgreSQL database และตั้งค่า `DATABASE_URL` อัตโนมัติ

โค้ดจะ:
- ใช้ PostgreSQL ถ้ามี `DATABASE_URL`
- ใช้ SQLite ถ้าไม่มี (ไม่แนะนำสำหรับ production)

## 🔐 Security

**สำคัญ:** หลังจาก deploy สำเร็จ:
1. Login เป็น admin (username: `admin`, password: `admin123`)
2. **เปลี่ยนรหัสผ่านทันที!**
3. ตรวจสอบว่า `SECRET_KEY` ถูกตั้งค่าแล้ว

## 📊 Monitoring

Railway มี built-in monitoring:
- **Logs**: ดู logs ได้ใน Dashboard → Deployments → Logs
- **Metrics**: ดู CPU, Memory, Network usage
- **Alerts**: ตั้งค่า alerts เมื่อมีปัญหา

## 🔄 Auto-Deploy

Railway จะ auto-deploy เมื่อ:
- Push code ไปที่ GitHub
- Merge Pull Request (ถ้าตั้งค่าไว้)

## 🛠️ Troubleshooting

### Build Fails
- ตรวจสอบว่า `requirements.txt` มี dependencies ครบ
- ตรวจสอบ logs ใน Railway Dashboard

### Database Connection Error
- ตรวจสอบว่า PostgreSQL service ถูกสร้างแล้ว
- ตรวจสอบว่า `DATABASE_URL` ถูกตั้งค่าแล้ว
- ตรวจสอบ logs สำหรับ error messages

### App Not Starting
- ตรวจสอบว่า `Procfile` หรือ start command ถูกต้อง
- ตรวจสอบ logs ใน Railway Dashboard
- ตรวจสอบว่า PORT environment variable ถูกใช้

### Migration Issues
- ตรวจสอบ logs สำหรับ migration errors
- Database จะถูก migrate อัตโนมัติเมื่อ app start ครั้งแรก

## 💰 Pricing

Railway มี:
- **Free Tier**: $5 credit/month (พอสำหรับ small apps)
- **Pro**: Pay-as-you-go
- **Team**: สำหรับทีม

ดู pricing เพิ่มเติม: [railway.app/pricing](https://railway.app/pricing)

## 📚 Resources

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Railway Status](https://status.railway.app)

## ✅ Checklist

- [ ] สร้าง Railway account
- [ ] Connect GitHub repository
- [ ] เพิ่ม PostgreSQL database
- [ ] ตั้งค่า `SECRET_KEY`
- [ ] Deploy application
- [ ] ตรวจสอบ logs
- [ ] Login และเปลี่ยนรหัสผ่าน admin
- [ ] ทดสอบการทำงานของระบบ

## 🎉 Success!

หลังจาก deploy สำเร็จ คุณจะได้:
- Public URL สำหรับเข้าถึง application
- Auto-deploy เมื่อ push code
- Database management
- Monitoring และ logging

---

**หมายเหตุ:** Railway จะ auto-detect Python และใช้ `requirements.txt` และ `Procfile` อัตโนมัติ ไม่ต้องตั้งค่าอะไรเพิ่มเติม!

